<%= form_for :message, url: " /messages/send_msg/#{@employee.id}", method: 'post' do |f| %>
	<p>
		Message:<br/>
		<%= f.text_area :message %>
	</p>
	
	<p>
		<%= f.submit :send%>

	</p>

<% end %>
<%= link_to 'Back', messages_path %>